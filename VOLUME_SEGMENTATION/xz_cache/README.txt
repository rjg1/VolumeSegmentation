This folder is where projected XZ planes for a run are stored, allowing them to be quickly retrieved for future segmentations.

Set the folder for a run using the `XZ_IO_PATH` global variable in `volume_segmentation.py`